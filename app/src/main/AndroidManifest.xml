<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gr.kalymnos.sk3m3l10.ddosdroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mvc_controllers.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mvc_controllers.activities.CreateAttackActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".mvc_controllers.activities.AllAttackListsActivity" />
        <activity android:name=".mvc_controllers.activities.JoinAttackActivity" />

        <service
            android:name=".mvc_model.attack.service.AttackService"
            android:enabled="true"
            android:exported="false" />
        <service
            android:name=".mvc_model.attack.connectivity.server.ServersHost"
            android:enabled="true"
            android:exported="false" />

        <activity android:name=".mvc_controllers.activities.WifiScanResultsActivity" />
        <activity android:name=".mvc_model.attack.connectivity.server.bluetooth.constraints.EnableBluetoothActivity"></activity>
    </application>

</manifest>